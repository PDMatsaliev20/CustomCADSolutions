@inject IViewLocalizer Localizer
@{
    int id = ViewBag.Id;
    string area = ViewBag.Area;
}

<a asp-area="@area" asp-controller="Orders" asp-action="Order" asp-route-id="@id" 
        class="btn btn-secondary text-decoration-none">
    @Localizer["Order", Model.Price]
</a>
